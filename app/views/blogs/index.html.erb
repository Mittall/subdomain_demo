<p id="notice"><%= notice %></p>

Host : <%= request.host.inspect %> <br/>
request.subdomain: <%= request.subdomain %> <br/>
Domain : <%= request.domain.inspect %> <br/>
Host Test : <%= request.host.include?('.blogs').inspect %> <br/>
Current subdomain : <%= current_subdomain.inspect %>

<h1>Listing Blogs</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Subdomain</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @blogs.each do |blog| %>
      <tr>
        <td><%= link_to blog.name, blog_path(blog) %></td>
        <td><%= blog.subdomain %></td>
        <td><%= link_to 'Show', blog %></td>
        <td><%= link_to 'Edit', edit_blog_path(blog) %></td>
        <td><%= link_to 'Destroy', blog, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Blog', new_blog_path %>
